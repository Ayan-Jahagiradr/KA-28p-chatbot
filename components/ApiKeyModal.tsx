import React from 'react';

// This component is no longer used because the API key is now managed
// via environment variables. It is neutralized to prevent accidental use.
const ApiKeyModal: React.FC = () => null;

export default ApiKeyModal;