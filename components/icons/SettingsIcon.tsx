import React from 'react';

// This component is no longer used after the removal of the settings button
// from the sidebar. It is neutralized to prevent accidental use.
const SettingsIcon = () => null;

export default SettingsIcon;